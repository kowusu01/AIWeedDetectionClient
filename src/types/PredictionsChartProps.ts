import PredictionsProps from "./PredictionsProps";

// Define the props interface
interface PredictionsChartProps {
  predictionsList: PredictionsProps[];
  summary: string;
}
export default PredictionsChartProps;
